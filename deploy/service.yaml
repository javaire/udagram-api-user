apiVersion: v1
kind: Service
metadata:
  name: udagram-api-user-service
  labels:
    app: udagram-api-user-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    app: udagram-api-user-service